"""
Управление ордерами (OrderManager).

Отвечает за выставление, переставление, отмену лимитных ордеров.
Контролирует очерёдность исполнения ног стратегии и порционный набор позиции.

Версия для Этапа 1: ЗАГЛУШКА.
Полная реализация — Этап 2 (Ядро стратегий).
"""

import logging

logger = logging.getLogger(__name__)


class OrderManager:
    """
    Менеджер ордеров (заглушка для Этапа 1).

    Логика (из ТЗ, раздел 5.2):
        - Все ордера — исключительно лимитные
        - Цена ордера рассчитывается через теор. цену (Блэк-76)
        - Ноги исполняются последовательно согласно очерёдности
        - Набор порциями с контролем максимального количества контрактов
        - При изменении IV ордер переставляется
    """

    def __init__(self, event_bus: "EventBus", config: dict):  # noqa: F821
        self._event_bus = event_bus
        self._config = config
        logger.info("OrderManager: ЗАГЛУШКА (реализация — Этап 2)")
