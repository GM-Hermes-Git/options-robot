"""
Модуль управления рисками (RiskManager).

Контролирует:
    - Стоп-лосс (SL) — закрытие позиции при убытке > SL%
    - Тейк-профит (TP) — закрытие позиции при прибыли > TP%
    - Защиту от переполнения (макс. количество контрактов)
    - Контроль времени активности стратегии

Версия для Этапа 1: ЗАГЛУШКА.
Полная реализация — Этап 4 (Хеджер и риски).
"""

import logging

logger = logging.getLogger(__name__)


class RiskManager:
    """
    Менеджер рисков (заглушка для Этапа 1).

    Логика (из ТЗ, раздел 8):
        - SL/TP в % от теоретической стоимости позиции на момент открытия
        - Закрытие лимитными ордерами в обратной очерёдности ног
        - Проверка лимита контрактов перед каждым ордером
        - Автоматическая остановка по истечении даты/времени окончания
    """

    def __init__(self, event_bus: "EventBus", config: dict):  # noqa: F821
        self._event_bus = event_bus
        self._config = config
        logger.info("RiskManager: ЗАГЛУШКА (реализация — Этап 4)")
